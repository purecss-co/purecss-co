type PureCSSProps = {
  title: string;
  image: string;
  youtube?: string;
  codepen: string;
  github: string;
  website: string;
  design: string;
};

type PureCSSData = {
  purecss: Array<PureCSSProps>;
};

export default PureCSSData;
